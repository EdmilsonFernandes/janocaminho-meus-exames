# Núcleo (treino + inferência + regras)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0
joblib>=1.3

# Endpoint interno de inferência (opcional — só p/ servir a API)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
