# URL base da API (backend). Em dev local: http://localhost:4001/api
# Para o app mobile, aponte para a URL pública/HTTPS do backend.
VITE_API_URL=http://localhost:4001/api

# Client ID do app iOS (GoogleService-Info.plist → CLIENT_ID) p/ login Google nativo no iOS
VITE_GOOGLE_IOS_CLIENT_ID=
